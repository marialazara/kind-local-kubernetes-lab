apiVersion: v1
kind: Service
metadata:
  name: hello-kind
  labels:
    app: hello-kind
spec:
  type: NodePort
  selector:
    app: hello-kind
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
